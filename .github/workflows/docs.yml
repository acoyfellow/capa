name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/docs.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: cd docs && bun run build
      - uses: actions/upload-artifact@v4
        with:
          name: docs-dist
          path: docs/dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      deployments: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: docs-dist
          path: dist
      - name: Deploy to Cloudflare Pages
        if: ${{ secrets.CLOUDFLARE_API_TOKEN != '' }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: bfcb6ac5b3ceaf42a09607f6f7925823
          command: pages deploy dist --project-name capa-docs --branch main
      - name: Pages token unavailable — build artifact is ready
        if: ${{ secrets.CLOUDFLARE_API_TOKEN == '' }}
        run: echo 'CLOUDFLARE_API_TOKEN is not set for Actions; skipping Pages deploy after a successful build.'
